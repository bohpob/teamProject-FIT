image: gradle:latest

stages:
  - build

variables:
  GRADLE_USER_HOME: "$CI_PROJECT_DIR/.gradle"

before_script:
  - gradle --version

build:
  stage: build
  script:
    - gradle build
  artifacts:
    paths:
      - build/libs/*.jar
